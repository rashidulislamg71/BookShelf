

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "encrypted-tbn0.gstatic.com",
      "i.pravatar.cc",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;